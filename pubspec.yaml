name: app_peliculas
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  image_picker: ^1.1.2
  supabase_flutter: ^2.9.1
  youtube_player_flutter: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/noirflix.jpg"

flutter:
  uses-material-design: true
  assets:
    - assets/noirflix.jpg
    - assets/data/peliculas.json

dependency_overrides:
  flutter_inappwebview: ^6.1.5
